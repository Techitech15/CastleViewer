"use strict";

/* ====================================================================
 * 1.5 nature / landscape decoration — castle-agnostic.
 *
 * Placeholder: the real implementation (clouds, birds, trees, groves,
 * lakes) is being written separately. Keeping a no-op stub here means
 * index.html can reference the file before it exists in full without a
 * 404 in the console.
 * ==================================================================== */
function updateNature(dt){ /* no-op until implemented */ }
function regenerateNature(){ /* no-op until implemented */ }
